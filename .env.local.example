# Local Supabase Database Connection Configuration
# Copy this file to .env.local and update as needed

# For local Supabase instances, the pooler requires a tenant ID in the username
# Format: postgres.{tenant-id}:password@host:port/database
DB_CONNECTION_STRING_SESSION_POOLER=postgresql://postgres.your-tenant-id:dev@127.0.0.1:5433/postgres?sslmode=disable

# Alternative connection options:
# Direct to database (if port 5432 is exposed):
# DB_CONNECTION_STRING_SESSION_POOLER=postgresql://postgres:dev@127.0.0.1:5432/postgres?sslmode=disable

# Transaction pooler (port 6543):
# DB_CONNECTION_STRING_SESSION_POOLER=postgresql://postgres.your-tenant-id:dev@127.0.0.1:6543/postgres?sslmode=disable

# Notes:
# - The default tenant ID in local Supabase is "your-tenant-id"
# - SSL is disabled for local development (sslmode=disable)
# - The session pooler runs on port 5433
# - The transaction pooler runs on port 6543
# - The database itself (if exposed) would be on port 5432